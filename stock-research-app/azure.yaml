# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: stock-research-app
metadata:
  template: stock-research-app

infra:
  provider: bicep
  path: infra

services:
  api:
    project: ./api
    language: python
    host: function
  
  web:
    project: ./web
    language: ts
    host: staticwebapp
    dist: dist
